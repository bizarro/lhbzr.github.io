div(class=`Wrapper ${styles.wrapper}`)
  h1(class=`Title ${styles.title}`) About Me

  p(class=`Description ${styles.description}`)
    | I'm Luis Henrique Bizarro, I'm a 24 y/o Creative Developer focused on WebGL, GLSL and JavaScript.
    | I'm currently working at Active Theory 🖤 and I'm also a Developer Jury member of Awwwards.

  p(class=`Description ${styles.description}`)
    | In my projects, to have more control over the application, I usually write my own code from scratch without any frameworks, just using the essential libaries with ECMAScript 2015+ features like Classes, Modules and Promises plus a simple Webpack build.

  div(class=`Social ${styles.social}`)
    span(class=styles.social__title) Get In Touch

    ul(class=styles.social__list)
      each social, index in socials
        li(class=styles.social__item)
          a(class=styles.social__link href=social.url target="_blank")=social.label
